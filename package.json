{
  "name": "test-plus-plus",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start:host": "func start --prefix future_dist",
    "start": "npm run start:host & npm run watch",
    "start:express": "npm run prestart && func start --prefix future_express_dist",
    "build:production": "npm run prestart && npm prune --production",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "azure-function-express": "^2.0.0",
    "cookie-parser": "^1.4.4",
    "express": "^4.16.4"
  },
  "devDependencies": {
    "@types/node": "^11.13.10",
    "@azure/functions": "^1.0.2-beta2",
    "nunjucks": "^3.2.0",
    "typescript": "^3.3.3"
  }
}
